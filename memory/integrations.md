# Integrations

Status: blank.

Record read-only findings about systems connected to this Salesforce org.

Include:

- connected apps, named credentials, auth providers, external credentials
- integration users and their purpose
- packages, namespaces, remote sites, webhooks, and API consumers
- sync direction, ownership, schedules, and retry behavior when known
- fields or objects controlled by integrations
- data that should not be manually edited because an integration owns it
- confidence and evidence for each finding

Do not store secrets, tokens, client secrets, certificates, or private keys.
